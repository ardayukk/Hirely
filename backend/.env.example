# Example environment variables

# Option A: Provide a full connection string
# DATABASE_URL=postgresql+psycopg://postgres:postgres@localhost:5432/hirely

# Option B: Provide discrete DB_* vars (used to build DATABASE_URL automatically)
DB_NAME=hirely
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432
